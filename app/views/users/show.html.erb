<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>

<%= button_to 'Events', events_path, :method => :get %>
<%= button_to 'Profile', user_profile_path(params[:id], @user.profile.id), :method => :get %>
